# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

The project rules live in `AGENTS.md` so that Cursor reads them natively. This file imports the
same rules so Claude Code loads them too — keep the content there, not here.

@AGENTS.md
